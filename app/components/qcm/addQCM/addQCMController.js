'use strict';
controllers.controller('AddQCMCtrl', ['$scope','dialogs','QcmDetailService','$routeParams','$location',
    function ($scope, dialogs, AddQCMService, $routeParams, $location) {

        $scope.qcm={};

    }]);

